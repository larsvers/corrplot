import dat from 'dat.gui';

const params = {
  x: 0,
  y: 0,
  z: 0,
  up: 1,
};

// const gui = new dat.GUI();
// gui.add(params, 'x', 0, 360, 1);
// gui.add(params, 'y', 0, 360, 1);
// gui.add(params, 'z', 0, 360, 1);
// gui.add(params, 'up', 0, 1, 0.1);

export default params;
